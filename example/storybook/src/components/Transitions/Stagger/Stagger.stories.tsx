import type { ComponentMeta } from '@storybook/react-native';
import Stagger from './Stagger';

const StaggerMeta: ComponentMeta<typeof Stagger> = {
  title: 'stories/TRANSITIONS/Stagger',
  component: Stagger,
  argTypes: {},
  args: {},
};

export default StaggerMeta;

export { Stagger };
