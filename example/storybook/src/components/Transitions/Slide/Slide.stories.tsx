import type { ComponentMeta } from '@storybook/react-native';
import Slide from './Slide';

const SlideMeta: ComponentMeta<typeof Slide> = {
  title: 'stories/TRANSITIONS/Slide',
  component: Slide,
  argTypes: {},
  args: {},
};

export default SlideMeta;

export { Slide };
