import type { ComponentMeta } from '@storybook/react-native';
import ZStack from './ZStack';

const ZStackMeta: ComponentMeta<typeof ZStack> = {
  title: 'stories/LAYOUT/ZStack',
  component: ZStack,
  argTypes: {},
  args: {},
};

export default ZStackMeta;

export { ZStack };
