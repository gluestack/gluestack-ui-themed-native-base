import type { ComponentMeta } from '@storybook/react-native';
import Skeleton from './Skeleton';

const SkeletonMeta: ComponentMeta<typeof Skeleton> = {
  title: 'stories/FEEDBACK/Skeleton',
  component: Skeleton,
  argTypes: {},
  args: {},
};

export default SkeletonMeta;

export { Skeleton };
